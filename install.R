# Run this file before running the app - it will install the packages you need.
# This might take a while, the console will show a > symbol at the bottom when the installation is finished.
# You will only need to run it once.

install.packages("dplyr")
install.packages("shiny")
install.packages("rhandsontable")